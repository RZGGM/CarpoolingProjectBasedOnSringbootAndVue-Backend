<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzasc.onlinecarhailing.Mapper.OrderMapper">


    <insert id="insertOrder">
        insert into `order`(order_id, create_time, finish_time, driver_id, man_count, passenger_id, tips, price,
                            type, owner_id, state, ticket_id, departure, destination, create_user_type, passenger_count,
                            join_order_can_join, other_id, phone)
        values (#{orderId}, NOW(), #{finishTime}, #{driverId}, #{manCount}, #{passengerId}, #{tips}, #{price},
                #{orderType}, #{ownerId},
                #{state}, #{ticketId}, #{departureAddress}, #{destinationAddress}, #{createUserType}, #{passengerCount},
                #{joinOrderCanJoin}, #{otherId}, #{phone})
    </insert>

    <update id="updateOrder">
        update `order`
        <set>
            <if test="state != null">
                state = #{state}
            </if>
        </set>
        where order_id = #{orderId}
    </update>
    <update id="updateOrderAppraise">
        update `order`
        set appraise_id = #{appraiseId}
        where order_id = #{orderId}
    </update>
    <update id="updateOrderDriverOrPassenger">
        update `order`
        <set>
            <if test="driverId != null">
                driver_id = #{driverId},
            </if>
            <if test="passengerId != null and passengerId != -1">
                passenger_id = #{passengerId},
            </if>
            <if test="appraiseId != null">
                appraise_id = #{appraiseId},
            </if>
            <if test="orderType != null">
                type = #{orderType},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="otherId != null">
                other_id = #{otherId},
            </if>
            <if test="manCount != null">
                man_count = #{manCount},
            </if>
            <if test="phone != null">
                phone = #{phone}
            </if>
        </set>
        where order_id = #{orderId}
    </update>
    <update id="updateOrderFinishTime">
        update `order`
        set finish_time = NOW()
        where order_id = #{orderId}


    </update>


    <delete id="deleteOrderById">
        delete
        from `order`
        where owner_id = #{id}
    </delete>
    <delete id="deleteOrdersByIds">
        delete from `order` where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>

    </delete>
    <resultMap id="orderMap" type="com.gzasc.onlinecarhailing.pojo.Order">
        <result column="type" property="orderType"/>
        <result column="departure" property="departureAddress"/>
        <result column="destination" property="destinationAddress"/>
    </resultMap>

    <select id="selectBySelfId" resultType="com.gzasc.onlinecarhailing.pojo.Order" resultMap="orderMap">
        select *
        from `order`
        where owner_id = #{userId}
        order by create_time desc
    </select>

    <select id="selectByOrderId" resultType="com.gzasc.onlinecarhailing.pojo.Order" resultMap="orderMap">

        select *
        from `order`
        where order_id = #{orderId}
        order by create_time desc

    </select>

    <!--    根据订单的状态来返回-->

    <select id="selectByOrderState" resultType="com.gzasc.onlinecarhailing.pojo.Order" resultMap="orderMap">
        select * from `order`
        <where>
            state = #{state}
        </where>

        order by create_time desc
    </select>
    <select id="selectByOrderType" resultType="com.gzasc.onlinecarhailing.pojo.Order" resultMap="orderMap">
        select * from `order`
        <where>
            type = #{orderType}
        </where>

        order by create_time desc
    </select>
    <select id="selectByCreateUserTypeAndOwnerId" resultType="com.gzasc.onlinecarhailing.pojo.Order"
            resultMap="orderMap">

        select *
        from `order`
        where create_user_type = #{createUserType}
          and owner_id = #{ownerId}
        order by create_time desc
    </select>

    <select id="selectByOrderStateAndDriverId" resultType="com.gzasc.onlinecarhailing.pojo.Order">
        select * from `order`
        <where>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="driverId != null">
                and driver_id != #{driverId}
            </if>

        </where>
        order by create_time desc
    </select>
    <select id="selectByOrderStateAndPassengerId" resultType="com.gzasc.onlinecarhailing.pojo.Order">
        select * from `order`
        <where>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="passengerId != null">
                and driver_id != #{passengerId}
            </if>

        </where>
        order by create_time desc
    </select>

</mapper>